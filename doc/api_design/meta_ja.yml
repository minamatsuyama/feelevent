---
"$schema": http://json-schema.org/draft-04/hyper-schema
id: api
title:       "FeelGood API v1"
description: "FeelGood API v1 インターフェース仕様(JSON Hyper Schema draft v4 形式で記述しています)"
type:
  - object
links:
  - href: https://v2.feelnote.org
    rel:  self

# グローバル変数として定義する
definitions:
  # 言語は全てでまたいで利用するため以下のようにしておく
  # LANG_CODE の参照名にするためネストしている
  lang:
    description: 言語
    definitions:
      code:
        description: 各言語を指定するコード
        type:        string
        example:     ja
        pattern:     (ja)
    type: object
  site:
    description: "サイト(法人)"
    definitions:
      code:
        description: サイト(法人)を識別するコード
        type:        string
        example:     1pac
    type: object
